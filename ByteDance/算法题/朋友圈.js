/**
 * @param {number[][]} M
 * @return {number}
 */
var findCircleNum = function(M) {
  // 行
  let rows = M.length;
  if (rows === 0 || M[0].length === 0) {
    return 0;
  }
  let cols = M[0].length;
  for (let i = 0; i < rows; i++) {
      
  }
};
