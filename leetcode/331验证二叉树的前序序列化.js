/**
 * @param {string} preorder
 * @return {boolean}
 */
var isValidSerialization = function(preorder) {
  let str = preorder.split(",");
  let len = str.length;
  
};

const judge = (root,left,right) => {
    
}
