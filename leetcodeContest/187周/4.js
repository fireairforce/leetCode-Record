/**
 * @param {number[][]} mat
 * @param {number} k
 * @return {number}
 */
// 行合并，每一轮结果只保留k个
var kthSmallest = function (mat, k) {
  let m = mat.length
  let n = mat[0].length
  for (let i = 0; i < m; i++) {
    for (let j = 0; j < n; j++) {
      
    }
  }
}
