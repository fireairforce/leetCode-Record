/**
 * @param {number[]} time
 * @param {number} m
 * @return {number}
 */
var minTime = function (time, m) {
  if (time.length < m) {
    return 0
  }
  for (let i = 0; i < time.length; i++) {
    sum += time[i]
  }
  
}
